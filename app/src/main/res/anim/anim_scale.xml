<?xml version="1.0" encoding="utf-8"?>
<scale xmlns:android="http://schemas.android.com/apk/res/android"
    android:interpolator="@android:anim/accelerate_interpolator"
    android:fromXScale="0.2"
    android:toXScale="1.5"
    android:fromYScale="0.2"
    android:toYScale="1.5"
    android:pivotX="50%"
    android:pivotY="50%"
    android:duration="2000">
</scale>


    <!--fromXScale/fromYScale：沿着X轴/Y轴缩放的起始比例,浮点值，比如1.0代表自身无变化，0.5表示缩小一倍，2.0表示放大一倍-->
    <!--toXScale/toYScale：沿着X轴/Y轴缩放的结束比例， 同上-->
    <!--pivotX/pivotY：缩放的中轴点X/Y坐标，即距离自身左边缘的位置，
        可以使数值、百分数、百分数p三种样式，比如50、50%、50%p，当为数值时，表示在当前view的左上角，即原点出加上50px，作为其实缩放点；
        如果是50%，表示在当前控件的左上角加上自己的宽度/高度50%作为起始点；
        如果是50%p，那么就是表示在当前的左上角加上父控件的50%的宽度/高度作为起始点X/Y轴坐标
    -->

    <!-- Animation 基类记录于 anim_interface中 -->
